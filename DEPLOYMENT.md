# 🚀 Инструкция по развертыванию на GitHub Pages

## Пошаговое руководство для новичков

### Шаг 1: Подготовка GitHub аккаунта

1. **Создайте аккаунт на GitHub** (если еще нет):
   - Перейдите на https://github.com/
   - Нажмите "Sign up"
   - Следуйте инструкциям регистрации

2. **Установите Git** (если еще нет):
   
   **Windows:**
   - Скачайте Git с https://git-scm.com/download/win
   - Установите с настройками по умолчанию
   
   **macOS:**
   ```bash
   # Используя Homebrew
   brew install git
   ```
   
   **Linux (Ubuntu/Debian):**
   ```bash
   sudo apt-get update
   sudo apt-get install git
   ```

3. **Настройте Git:**
   ```bash
   git config --global user.name "Ваше Имя"
   git config --global user.email "your.email@example.com"
   ```

### Шаг 2: Создание репозитория на GitHub

**Вариант A: Через веб-интерфейс GitHub**

1. Войдите в GitHub
2. Нажмите "+" в правом верхнем углу → "New repository"
3. Заполните форму:
   - **Repository name**: `mcp-code-execution-ru` (или любое другое имя)
   - **Description**: "Транскрипт блога Anthropic о MCP на русском"
   - **Public** (обязательно для GitHub Pages)
   - НЕ добавляйте README, .gitignore или лицензию (они уже есть в проекте)
4. Нажмите "Create repository"

**Вариант B: Через GitHub CLI (если установлен gh)**

```bash
cd /path/to/webapp
gh repo create mcp-code-execution-ru --public --source=. --push
```

### Шаг 3: Загрузка кода на GitHub

1. **Откройте терминал** в папке проекта:
   ```bash
   cd /path/to/webapp
   ```

2. **Убедитесь, что Git инициализирован:**
   ```bash
   git status
   ```
   Если видите "not a git repository", выполните:
   ```bash
   git init
   ```

3. **Добавьте все файлы:**
   ```bash
   git add .
   ```

4. **Создайте первый коммит:**
   ```bash
   git commit -m "Initial commit: MCP Code Execution website"
   ```

5. **Переименуйте ветку в main (если нужно):**
   ```bash
   git branch -M main
   ```

6. **Подключите удаленный репозиторий:**
   ```bash
   # Замените YOUR_USERNAME на ваше имя пользователя GitHub
   git remote add origin https://github.com/YOUR_USERNAME/mcp-code-execution-ru.git
   ```

7. **Загрузите код на GitHub:**
   ```bash
   git push -u origin main
   ```
   
   При первой загрузке GitHub попросит авторизацию:
   - **Способ 1**: Используйте Personal Access Token (рекомендуется)
     - Перейдите: Settings → Developer settings → Personal access tokens → Tokens (classic)
     - Нажмите "Generate new token"
     - Выберите scope "repo"
     - Скопируйте токен и используйте его вместо пароля
   
   - **Способ 2**: Используйте GitHub CLI
     ```bash
     gh auth login
     ```

### Шаг 4: Активация GitHub Pages

1. **Перейдите в настройки репозитория:**
   - Откройте ваш репозиторий на GitHub
   - Нажмите "Settings" (вкладка справа)

2. **Найдите раздел Pages:**
   - В левом меню выберите "Pages"
   
3. **Настройте источник:**
   - **Source**: Выберите "Deploy from a branch"
   - **Branch**: Выберите "main"
   - **Folder**: Выберите "/ (root)"
   - Нажмите "Save"

4. **Подождите несколько минут:**
   - GitHub Pages соберет и опубликует ваш сайт
   - Вы увидите сообщение с URL: `https://YOUR_USERNAME.github.io/mcp-code-execution-ru/`

5. **Проверьте развертывание:**
   - Нажмите на вкладку "Actions" в репозитории
   - Дождитесь завершения workflow "pages build and deployment"
   - Зеленая галочка = успешное развертывание

### Шаг 5: Проверка работы сайта

1. Откройте URL вашего сайта в браузере
2. Проверьте:
   - ✅ Сайт загружается
   - ✅ Все стили применены
   - ✅ Интерактивные элементы работают
   - ✅ Код подсвечивается
   - ✅ Анимации работают при прокрутке

### Шаг 6: Обновление сайта

Когда вы вносите изменения:

```bash
# Посмотрите, что изменилось
git status

# Добавьте изменения
git add .

# Создайте коммит с описанием изменений
git commit -m "Описание изменений"

# Загрузите на GitHub
git push origin main
```

GitHub Pages автоматически пересоберет и опубликует обновленную версию.

## 🔧 Дополнительные настройки

### Настройка кастомного домена

Если у вас есть свой домен:

1. **Настройте DNS:**
   Добавьте A-записи:
   ```
   185.199.108.153
   185.199.109.153
   185.199.110.153
   185.199.111.153
   ```

2. **Обновите файл CNAME:**
   ```bash
   echo "your-domain.com" > CNAME
   git add CNAME
   git commit -m "Add custom domain"
   git push origin main
   ```

3. **Настройте в GitHub:**
   - Settings → Pages → Custom domain
   - Введите ваш домен
   - Нажмите "Save"
   - Включите "Enforce HTTPS" (после проверки домена)

### Настройка SEO

1. **Обновите robots.txt и sitemap.xml:**
   - Замените `YOUR_USERNAME` на ваше имя пользователя
   - Укажите правильный URL

2. **Добавьте Google Search Console:**
   - Перейдите на https://search.google.com/search-console
   - Добавьте ваш сайт
   - Отправьте sitemap.xml

3. **Добавьте Yandex Webmaster:**
   - Перейдите на https://webmaster.yandex.ru/
   - Добавьте ваш сайт
   - Отправьте sitemap.xml

## 🐛 Решение проблем

### Сайт не отображается (404 ошибка)

**Решение:**
1. Проверьте, что GitHub Pages активирован (Settings → Pages)
2. Убедитесь, что выбрана ветка "main" и папка "/ (root)"
3. Проверьте URL - он должен содержать имя репозитория
4. Подождите 5-10 минут после первого деплоя

### Стили не применяются

**Решение:**
1. Проверьте, что используются CDN-ссылки для Tailwind CSS
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь, что файл index.html находится в корне репозитория

### Git push требует авторизацию

**Решение:**
1. Используйте Personal Access Token вместо пароля
2. Или настройте SSH-ключи:
   ```bash
   ssh-keygen -t ed25519 -C "your.email@example.com"
   cat ~/.ssh/id_ed25519.pub
   # Добавьте вывод в Settings → SSH and GPG keys на GitHub
   
   # Измените remote на SSH
   git remote set-url origin git@github.com:YOUR_USERNAME/mcp-code-execution-ru.git
   ```

### Изменения не применяются

**Решение:**
1. Проверьте, что коммит загружен: `git log`
2. Проверьте Actions на GitHub - возможно, сборка не удалась
3. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
4. Подождите несколько минут - GitHub Pages может занять время

## 📊 Мониторинг сайта

### Google Analytics (опционально)

Добавьте в `<head>` в index.html:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

### Yandex.Metrika (опционально)

Добавьте в `<head>` в index.html:

```html
<!-- Yandex.Metrika counter -->
<script type="text/javascript" >
   (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   m[i].l=1*new Date();
   for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
   k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
   (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

   ym(XXXXXX, "init", {
        clickmap:true,
        trackLinks:true,
        accurateTrackBounce:true
   });
</script>
```

## 🎉 Готово!

Ваш сайт теперь доступен публично и оптимизирован для поисковых систем. Поделитесь ссылкой с коллегами и сообществом MCP!

## 📞 Поддержка

Если возникли проблемы:
- 📖 Прочитайте [официальную документацию GitHub Pages](https://docs.github.com/en/pages)
- 🐛 Создайте Issue в репозитории проекта
- 💬 Обратитесь в сообщество MCP

---

**Удачи с развертыванием! 🚀**
